# Configuration variables
INTERFACE = "en0"         # Network interface to monitor (change to yours, e.g., "wlan0")
CAPTURE_DURATION = 5      # How long to capture packets (in seconds)
THREAT_THRESHOLD = 1     # Max ICMP packets from one IP before flagging as a threat
